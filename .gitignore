.DS_Store

# Dependencies
node_modules/

# Frontend build & test artifacts
frontend/dist/
frontend/coverage/
*.tsbuildinfo

# Go module cache (local)
backend/.gomodcache/

# Go test coverage
*.out

# Environment (local values / secrets — never commit)
.env
.env.local
.env.*.local

# Environment templates (definition examples — safe to commit)
!.env.example
!**/.env.example

# Editor / OS
.idea/
.vscode/
*.swp
*~
